[package]
name = "aitm-collector"
version = "0.1.0"
description = "AI Task Manager collector: a small self-hosted server that receives seat reports and shows a team dashboard."
edition = "2021"
license = "MIT"
publish = false

[dependencies]
aitm-core = { path = "../core" }
serde_json = "1"
tiny_http = "0.12"
chrono = "0.4"
