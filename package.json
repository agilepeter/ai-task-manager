{
  "name": "ai-task-manager",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "node --test scripts/*.test.mjs scripts/check-demo-fixture.mjs",
    "build:demo": "python3 scripts/make-demo-html.py && tsc && vite build --mode demo",
    "fixture:demo": "python3 scripts/make-demo-fixture.py",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6.0.3",
    "typescript": "~5.6.2"
  }
}
